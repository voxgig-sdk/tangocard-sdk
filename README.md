# Tangocard SDK

Tango API client, generated from the OpenAPI spec.

Welcome to the Tango API – with this RESTful API you can integrate a global reward or incentive program into your app or platform.<br /><br /> This console works in our Sandbox environment.

Learn more about Tango API at [integration-api.tangocard.com](https://integration-api.tangocard.com).

This is an unofficial SDK for the Tango public API, generated by Voxgig with [`@voxgig/sdkgen`](https://github.com/voxgig/sdkgen). It is not affiliated with, endorsed by, or sponsored by the upstream API provider.

Learn more about Voxgig SDKs at [voxgig.com/sdk](https://voxgig.com/sdk/).

Full documentation for this SDK: [https://voxgig-sdk.github.io/tangocard-sdk/](https://voxgig-sdk.github.io/tangocard-sdk/)

> TypeScript, Python, PHP, Golang, Lua, JavaScript SDKs, a CLI with an interactive REPL, and an MCP server for AI agents — all generated from one OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).

> **Features:** `debug`, `idempotency`, `metrics`, `paging`, `ratelimit`, `retry`, `test`, `timeout` — opt-in,
> inactive until switched on, and configured per client. See the Features
> section of any SDK README below for what each one does.

## Entities, not endpoints

This SDK exposes the API as **46 semantic entities** that you
call directly, instead of assembling URL paths and query strings. See the [Entities](#entities) table below for the full list. Entities are
**Capitalised** to mark them as the primary surface, each with the operations they
support (`list`, `load`, `create`, `update`, `remove`):

```ts
const client = new TangocardSDK()
const account = await client.Account().load({ id: "example_id" })
```

Thinking in entities keeps the mental model small — for people and AI agents alike —
rather than reasoning about raw HTTP routes and query parameters.

## Offline unit testing

Every SDK ships a built-in **test mode** that swaps the HTTP transport for
an in-memory mock, so your unit tests run fully offline — no server, no
network, and no credentials:

### TypeScript

```ts
// The offline mock starts EMPTY — seed it with the records the test needs.
// Shape: { entity: { <entity-name>: { <id>: <record> } } }
const client = TangocardSDK.test({
  entity: {
    reward_reasons_map: {
      test01: { id: 'test01' },
    },
  },
})
const rewardreasonsmap = await client.RewardReasonsMap().load()
// rewardreasonsmap is the RewardReasonsMap entity, populated with mock data
// — call rewardreasonsmap.data() for the record itself
console.log(rewardreasonsmap)
```

### Python

```python
client = TangocardSDK.test()
rewardreasonsmap = client.RewardReasonsMap().load()
print(rewardreasonsmap)
```

### PHP

```php
// Seed fixture data so offline calls resolve without a live server.
$client = TangocardSDK::test([
    "entity" => ["rewardreasonsmap" => ["test01" => []]],
]);
$rewardreasonsmap = $client->RewardReasonsMap()->load();
```

### Golang

```go
client := sdk.Test()
result, err := client.RewardReasonsMap(nil).Load(
    nil, nil,
)
```

### Lua

```lua
local client = sdk.test()
local result, err = client:RewardReasonsMap():load()
```

### JavaScript

```js
const client = TangocardSDK.test()
const rewardreasonsmap = await client.RewardReasonsMap().load()
// rewardreasonsmap is the entity, populated with mock data
// — call rewardreasonsmap.data() for the record itself
console.log(rewardreasonsmap)
```

## Packages

| Language | Package | Install |
| --- | --- | --- |
| TypeScript | `@voxgig-sdk/tangocard` | publish pending — [install from git tag](https://github.com/voxgig-sdk/tangocard-sdk/tags) |
| Python | `voxgig-sdk-tangocard` | publish pending — [install from git tag](https://github.com/voxgig-sdk/tangocard-sdk/tags) |
| PHP | `voxgig-sdk/tangocard` | publish pending — [install from git tag](https://github.com/voxgig-sdk/tangocard-sdk/tags) |
| Golang | `github.com/voxgig-sdk/tangocard-sdk/go` | `go get github.com/voxgig-sdk/tangocard-sdk/go@latest` |
| Lua | `voxgig-sdk-tangocard` | publish pending — [install from git tag](https://github.com/voxgig-sdk/tangocard-sdk/tags) |
| JavaScript | `@voxgig-sdk/tangocard-js` | publish pending — [install from git tag](https://github.com/voxgig-sdk/tangocard-sdk/tags) |
| Go CLI | `github.com/voxgig-sdk/tangocard-sdk/go-cli` | `go install github.com/voxgig-sdk/tangocard-sdk/go-cli/cmd/tangocard@latest` |
| Go MCP server | `github.com/voxgig-sdk/tangocard-sdk/go-mcp` | `go get github.com/voxgig-sdk/tangocard-sdk/go-mcp@latest` |

## Quickstart

### TypeScript

```ts
import { TangocardSDK } from '@voxgig-sdk/tangocard'

const client = new TangocardSDK({
  apikey: process.env.TANGOCARD_APIKEY,
  secret: process.env.TANGOCARD_SECRET,
})


// Load a specific asyncorderdetailview (returns a AsyncOrderDetailView)
const asyncorderdetailview = await client.AsyncOrderDetailView().load({
  account_identifier: 'example_account_identifier',
  customer_identifier: 'example_customer_identifier',
  external_ref_id: 'example_external_ref_id',
})
console.log(asyncorderdetailview)
```

See the [TypeScript README](ts/README.md) for the full guide.

## Surfaces

| Surface | Path |
| --- | --- |
| **SDK** (TypeScript, Python, PHP, Golang, Lua, JavaScript) | `ts/` `py/` `php/` `go/` `lua/` `js/` |
| **CLI** | `go-cli/` |
| **MCP server** | `go-mcp/` |

## Use it from an AI agent (MCP)

The generated MCP server exposes every operation in this SDK as an
[MCP](https://modelcontextprotocol.io) tool that Claude, Cursor or Cline
can call directly. Build and register it:

```bash
cd go-mcp && go build -o tangocard-mcp .
```

Then add it to your agent's MCP config (Claude Desktop, Cursor, etc.):

```json
{
  "mcpServers": {
    "tangocard": {
      "command": "/abs/path/to/tangocard-mcp"
    }
  }
}
```

## Entities

The API exposes 46 entities:

| Entity | Description | API path |
| --- | --- | --- |
| **Account** | The Account entity (load, update). | `/accounts` |
| **AddCommentEscalation** | The AddCommentEscalation entity (create). | `/prepaidCardService/addCommentEscalation/{referenceLineItemID}` |
| **AllEventType** | The AllEventType entity (list). | `/webhooks/eventtypes` |
| **AsyncOrder** | The AsyncOrder entity (create, list). | `/asyncOrders` |
| **AsyncOrderDetailView** | The AsyncOrderDetailView entity (load, update). | `/asyncOrders/customers/{customerIdentifier}/accounts/{accountIdentifier}/{externalRefID}` |
| **AsyncOrderLineItemsView** | The AsyncOrderLineItemsView entity (list). | `/asyncOrders/customers/{customerIdentifier}/accounts/{accountIdentifier}/{externalRefID}/lineItems` |
| **AsyncReasonCodesView** | The AsyncReasonCodesView entity (load). | `/asyncOrders/reasonCodes` |
| **AsyncUpdateLineItemView** | The AsyncUpdateLineItemView entity (update). | `/asyncOrders/lineItems/{referenceLineItemId}` |
| **BalanceAlertView** | The BalanceAlertView entity (remove). | `/customers/{customerIdentifier}/accounts/{accountIdentifier}/lowbalance/{balanceAlertID}` |
| **BrandCategoriesView** | The BrandCategoriesView entity (list). | `/brandCategories` |
| **Catalog** | The Catalog entity (list). | `/choiceProducts/{choiceProductUtid}/catalog` |
| **ChoiceProduct** | The ChoiceProduct entity (list, load). | `/choiceProducts` |
| **CountryViewSummary** | The CountryViewSummary entity (load). | `/rewardCountries` |
| **CreateAccountCriterion** | The CreateAccountCriterion entity (create). | `/customers/{customerIdentifier}/accounts` |
| **CreateCustomerCriterion** | The CreateCustomerCriterion entity. | `` |
| **CredentialTypeView** | The CredentialTypeView entity (list). | `/credentialtypes` |
| **CreditCard** | The CreditCard entity (create, load). | `/creditCards` |
| **CreditCardDeposit** | The CreditCardDeposit entity (create, load). | `/creditCardDeposits/{referenceDepositID}` |
| **CreditCardUnregister** | The CreditCardUnregister entity (create). | `/creditCardUnregisters` |
| **Customer** | The Customer entity (create, list, load). | `/customers` |
| **EmailTemplateListView** | The EmailTemplateListView entity. | `` |
| **EmailTemplateViewVerbose** | The EmailTemplateViewVerbose entity (create, list, load, update). | `/digitalTemplates` |
| **EmbeddableResponseDto** | The EmbeddableResponseDto entity (load). | `/lineItems/{referenceLineItemID}/embeddedUrl` |
| **ExchangeRatesWithDisclaimer** | The ExchangeRatesWithDisclaimer entity (list). | `/exchangerates` |
| **LineItem** | The LineItem entity (create, list, load). | `/lineItems` |
| **LowBalanceAlertListView** | The LowBalanceAlertListView entity (list). | `/customers/{customerIdentifier}/accounts/{accountIdentifier}/lowbalance` |
| **LowBalanceAlertView** | The LowBalanceAlertView entity (create, load, update). | `/customers/{customerIdentifier}/accounts/{accountIdentifier}/lowbalance/{balanceAlertID}` |
| **MobileCountry** | The MobileCountry entity (load). | `/mobileCountries` |
| **N14Webhook** | The N14Webhook entity (create, list, load, remove). | `/webhooks` |
| **N1Customer** | The N1Customer entity (load). | `/customers/{customerIdentifier}/accounts` |
| **N2Account** | The N2Account entity. | `` |
| **N3Fund** | The N3Fund entity. | `` |
| **N8LineItem** | The N8LineItem entity (update). | `/lineItems/{referenceLineItemID}` |
| **N9DigitalTemplate** | The N9DigitalTemplate entity (remove). | `/digitalTemplates/{etid}` |
| **Order** | The Order entity (create, list, load). | `/orders` |
| **OrderViewSummary** | The OrderViewSummary entity (create). | `/lineItems/{referenceLineItemID}/reissue` |
| **PrepaidCardInfo** | The PrepaidCardInfo entity (load). | `/prepaidCardService/getCardInfo/{referenceLineItemID}` |
| **PrepaidCardTransaction** | The PrepaidCardTransaction entity (load). | `/prepaidCardService/getCardTransactions/{referenceLineItemID}` |
| **ReissueCard** | The ReissueCard entity (create). | `/prepaidCardService/reissueCard/{referenceLineItemID}` |
| **ReplacementReason** | The ReplacementReason entity (list). | `/prepaidCardService/replacementReasons` |
| **Resend** | The Resend entity (create). | `/lineItems/{referenceLineItemId}/resends` |
| **RewardReasonsMap** | The RewardReasonsMap entity (load). | `/lineItems/reasonCodes` |
| **TransferFund** | The TransferFund entity (create). | `/transferFunds` |
| **UpdateAccount** | The UpdateAccount entity (create). | `/prepaidCardService/updateAccount/{referenceLineItemID}` |
| **UpdateWebhookSubscriptionResponseView** | The UpdateWebhookSubscriptionResponseView entity (update). | `/webhooks/{webhookId}` |
| **Webhook** | The Webhook entity (create, load). | `/webhooks/{webhookId}` |

The operations available across these entities are **load**, **list**, **create**, **update**, **remove** — see each entity's
own list above for exactly which it supports.

## Quickstart in other languages

### Python

```python
import os
from tangocard_sdk import TangocardSDK

client = TangocardSDK({
    "apikey": os.environ.get("TANGOCARD_APIKEY"),
})


# Load a specific account (returns the record, raises on error)
account = client.Account().load({"id": "example_id"})
print(account)
```

### PHP

```php
<?php
require_once 'tangocard_sdk.php';

$client = new TangocardSDK([
    "apikey" => getenv("TANGOCARD_APIKEY"),
]);


// Load a specific account (returns the ENTITY; call data_get() for the record; throws on error)
$account = $client->Account()->load(["id" => "example_id"]);
print_r($account->data_get());
```

### Golang

```go
import sdk "github.com/voxgig-sdk/tangocard-sdk/go"

client := sdk.NewTangocardSDK(map[string]any{
    "apikey": os.Getenv("TANGOCARD_APIKEY"),
})


// Load a specific asyncorderdetailview
asyncOrderDetailView, err := client.AsyncOrderDetailView(nil).Load(
    map[string]any{"account_identifier": "example_account_identifier", "customer_identifier": "example_customer_identifier", "external_ref_id": "example_external_ref_id"}, nil,
)
if err != nil {
    panic(err)
}
fmt.Println(asyncOrderDetailView)
```

### Lua

```lua
local sdk = require("tangocard_sdk")

local client = sdk.new({
  apikey = os.getenv("TANGOCARD_APIKEY"),
})


-- Load a specific account
local account, err = client:Account():load({ id = "example_id" })
print(account)
```

### JavaScript

```js
const { TangocardSDK } = require('@voxgig-sdk/tangocard-js')

const client = new TangocardSDK({
  apikey: process.env.TANGOCARD_APIKEY,
})


// Load a specific asyncorderdetailview (returns the entity)
const asyncorderdetailview = await client.AsyncOrderDetailView().load({
  account_identifier: 'example_account_identifier',
  customer_identifier: 'example_customer_identifier',
  external_ref_id: 'example_external_ref_id',
})
console.log(asyncorderdetailview)
```

## Direct and prepare

For endpoints the entity model doesn't cover, use the low-level methods:

- **`direct(fetchargs)`** — build and send an HTTP request in one step.
- **`prepare(fetchargs)`** — build the request without sending it.

Both accept a map with `path`, `method`, `params`, `query`,
`headers`, and `body`. See the [How-to guides](#how-to-guides) below.

## How-to guides

### Make a direct API call

When the entity interface does not cover an endpoint, use `direct`:

**TypeScript:**
```ts
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

**Python:**
```python
result = client.direct({
    "path": "/api/resource/{id}",
    "method": "GET",
    "params": {"id": "example"},
})
```

**PHP:**
```php
$result = $client->direct([
    "path" => "/api/resource/{id}",
    "method" => "GET",
    "params" => ["id" => "example"],
]);
```

**Go:**
```go
result, err := client.Direct(map[string]any{
    "path":   "/api/resource/{id}",
    "method": "GET",
    "params": map[string]any{"id": "example"},
})
if err != nil {
    panic(err)
}
fmt.Println(result)
```

**Lua:**
```lua
local result, err = client:direct({
  path = "/api/resource/{id}",
  method = "GET",
  params = { id = "example" },
})
```

**JavaScript:**
```js
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

## Advanced

> Everyday use only needs the sections above. This explains the internals
> behind every call — relevant when writing custom features.

Every SDK call runs the same five-stage pipeline:

1. **Point** — resolve the API endpoint from the operation definition.
2. **Spec** — build the HTTP specification (URL, method, headers, body).
3. **Request** — send the HTTP request.
4. **Response** — receive and parse the response.
5. **Result** — extract the result data for the caller.

A feature hook fires at each stage (e.g. `PrePoint`, `PreSpec`,
`PreRequest`), so features can inspect or modify the pipeline without
forking the SDK.

### Features

| Feature | Purpose |
| --- | --- |
| **DebugFeature** | Request/response capture ring buffer for debugging |
| **IdempotencyFeature** | Idempotency keys for safe retries of mutating operations |
| **MetricsFeature** | Statistics capture: per-operation counters and latency |
| **PagingFeature** | Pagination signals for list operations |
| **RatelimitFeature** | Client-side rate limiting via a token bucket |
| **RetryFeature** | Automatic retry of transient failures with exponential backoff |
| **TestFeature** | In-memory mock transport for testing without a live server |
| **TimeoutFeature** | Per-request timeout with transport abort |

Pass custom features via the `extend` option at construction time.

## Customizing this SDK

This repository contains its own generator (`.sdk/`), so the SDK is
customizable without forking any upstream tool:

- **The model** (`.sdk/model/`) declares everything this project owns:
  package names, versions, active features, per-target settings. It is
  written in [aontu](https://aontu.dev), a JSON-based
  specification language designed for building ontologies: easy to edit
  by hand, and files unify rather than override, so small declarations
  compose into one model. Regeneration re-reads it every time.
- **Templates** (`.sdk/tm/`) and **components** (`.sdk/src/cmp/`) are
  the two layers of generation, copied into this repo: templates are the
  literal per-language source, components generate the API-shaped parts.
- **Regeneration merges.** By default, newly generated content is
  three-way merged into existing files, so generator updates and local
  edits usually converge without manual conflict handling. A project can
  opt for plain overwrite instead.
- **Custom features and entire custom targets** arrive through sdkgen
  packages (`voxgig-sdkgen package add`), on the same rails as the
  bundled languages, and `voxgig-sdkgen doctor` reports any drift from
  what a resync would write.

How-to: [customize and propagate templates](https://github.com/voxgig/sdkgen/blob/main/docs/how-to/customize-and-propagate-templates.md).
The full story: [voxgig.com/sdk/custom](https://voxgig.com/sdk/custom).

## Per-language documentation

- [TypeScript](ts/README.md)
- [Python](py/README.md)
- [PHP](php/README.md)
- [Golang](go/README.md)
- [Lua](lua/README.md)
- [JavaScript](js/README.md)

## Upstream API

This SDK is generated from the upstream OpenAPI specification. It is an
unofficial client and is not affiliated with the API provider.

The OpenAPI spec(s) this SDK was generated from are kept in the
[`.sdk/def/`](.sdk/def/) folder.

- Upstream API: [https://www.tangocard.com/developer](https://www.tangocard.com/developer)

## Security

Please report security issues to security@voxgig.com. See [SECURITY.md](SECURITY.md).
Do not open public issues for suspected vulnerabilities.

---

Generated from the Tango API OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).
