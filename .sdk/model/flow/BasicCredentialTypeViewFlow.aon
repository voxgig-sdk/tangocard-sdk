# BasicCredentialTypeViewFlow

main: kit: flow: BasicCredentialTypeViewFlow:
{
  entity: "credential_type_view",
  kind: "basic",
  name: "BasicCredentialTypeViewFlow",
  step: [
    {
      op: "list",
      valid: [
        {
          apply: "ItemExists",
          def: {
            ref: "credential_type_view_ref01"
          }
        }
      ]
    }
  ],
  "key$": "BasicCredentialTypeViewFlow"
}